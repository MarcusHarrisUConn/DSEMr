## Development status

This package is not being submitted in its current development state. The file
exists so the final CRAN validation record can be written from actual check
logs rather than reconstructed later.


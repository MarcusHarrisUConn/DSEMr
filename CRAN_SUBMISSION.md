# CRAN submission status

**Not ready for submission.** DSEMr 0.0.1.9000 is the foundation development
release. Submission is blocked by the intentionally incomplete model-family
parity matrix in `docs/PARITY.md`, not by a claim that the current AR(1) engine
implements full DSEM.

Before submission, record package version, commit, operating systems, R
versions, Rust version, offline build proof, check results, reverse-dependency
results, public parity evidence, and all remaining notes here.


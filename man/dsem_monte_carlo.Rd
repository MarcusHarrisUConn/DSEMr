% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{dsem_monte_carlo}
\alias{dsem_monte_carlo}
\title{Run a reproducible DSEM Monte Carlo study}
\usage{
dsem_monte_carlo(
  model,
  replications = 100L,
  generator = function(seed) simulate_dsem(seed = seed),
  seed = 20260813L,
  compute = dsem_compute(),
  fit_args = list(chains = 2L, iter = 1000L, warmup = 500L),
  truth = NULL
)
}
\arguments{
\item{model}{A compiled model or model string accepted by \code{\link[=dsem]{dsem()}}.}

\item{replications}{Number of simulation replications.}

\item{generator}{Function called as \verb{generator(seed = <derived seed>)}.}

\item{seed}{Master simulation seed.}

\item{compute}{Compute specification. Independent replications can be
dispatched across a PSOCK cluster; deterministic seeds make results
invariant to worker count and job order.}

\item{fit_args}{Named list passed to \code{\link[=dsem]{dsem()}}.}

\item{truth}{Optional named vector of true parameter values.}
}
\value{
A \code{DSEMmontecarlo} list with replication estimates and summaries.
}
\description{
Run a reproducible DSEM Monte Carlo study
}
